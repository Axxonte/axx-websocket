ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        sslPort = 8443
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
    security {
        keystore = keystore.jks
        keyAlias = sampleAlias
        keyStorePassword = haribo7955
        privateKeyPassword = haribo7955
    }
}